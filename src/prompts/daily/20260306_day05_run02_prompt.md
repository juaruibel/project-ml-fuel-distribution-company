# Day 05.1 · prompt operativo

- `date_utc`: `2030-03-06`
- `day_id`: `Day 05.1`
- `base_prompt_ref`: `docs/context/proyecto_final.md`
- `version`: `1`
- `status`: `completed`

## Objetivo
Abrir una iteración corta y separada de Day 05 para aislar una única hipótesis metodológica: qué pasa si los árboles usan desde el arranque su mecanismo nativo de balanceo sobre la misma base post-fix y el mismo contrato de evaluación.

## Mensaje de transición
`Cerramos day 5.0 con métricas de modelos tabulares: hay un desplome en algunas métricas y mejoras claras en otras. Por ello iteraremos en day 5.1. para probar esta teoría`

## Alcance cerrado
1. Mantener exactamente la misma base post-fix, el mismo `cutoff_date = 2028-02-21`, el mismo holdout oficial, el mismo registry oficial y el mismo gate del repo.
2. No rehacer ETL, no abrir nuevas features, no abrir Brent y no tocar `notebook 16`.
3. Ejecutar una matriz corta de árboles balanceados nativamente:
   - `V2`
   - `V2_TRANSPORT_ONLY`
   - `V2_TRANSPORT_CARRY30D_ONLY`
4. Limitar familias a:
   - `LIGHTGBM` con `class_weight="balanced"`
   - `XGBOOST` con `scale_pos_weight`
5. No abrir todavía:
   - tuning amplio
   - `ROS`
   - `SMOTE`
   - ensemble
   - policy, salvo que aparezca un candidato claramente defendible

## Criterio de decisión
- `defendible`: pasa el gate oficial del repo
- `casi-gate`:
  - `delta_top2_vs_baseline >= -0.002`
  - `delta_bal_acc_vs_baseline >= -0.005`
  - `coverage >= baseline - 0.005`
  - `delta_bal_acc_vs_day05_unbalanced >= +0.030`
  - `delta_top2_vs_day05_unbalanced >= -0.003`

## Guardrails
- Day 05 queda congelado como `Day 5.0`.
- Day 05.1 complementa Day 05, no lo reescribe.
- Si no aparece ningún candidato defendible o casi-gate, la línea tabular se cierra y el siguiente bloque será Day 05.5.
